/**
 * 默认渲染缩放比例
 */
export const DEFAULT_SCALE = 1.5;

/**
 * 默认超时时间（毫秒）
 */
export const DEFAULT_TIMEOUT = 120_000; // 2 分钟

/**
 * 默认图片格式
 */
export const DEFAULT_FORMAT = 'png' as const;

/**
 * 默认 JPEG 质量
 */
export const DEFAULT_QUALITY = 80;

/**
 * 默认文件名模板
 */
export const DEFAULT_FILENAME_TEMPLATE = 'page-{page}.png';

/**
 * 默认输出目录名
 */
export const DEFAULT_OUTPUT_DIR = './pdf-screenshots';
